<section class="contact-hero" data-aos="fade-up">
    <div class="contact-hero__badge">
        <span>{{ __('START THE CLOCK') }}</span>
    </div>
    <div class="contact-hero__headline">
        <span>{{ __('INITIATE') }}</span>
        <span class="bg-clip-text text-transparent bg-gradient-primary">{{ __('CONTACT') }}</span>
    </div>
    <p class="contact-hero__copy">
        {{ __('Ready to completely dominate your market? Secure a consultation with our architects today.') }}
    </p>
</section>
